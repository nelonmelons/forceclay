/**
 * Base clay geometry construction and (de)serialization helpers (Task D).
 */
import type { SerializableGeometry } from "../types";

/** Builds a subdivided icosphere of the given radius/detail as starting clay geometry. */
export function makeClaySphere(_radius: number, _detail: number): SerializableGeometry {
  throw new Error("notImplemented: makeClaySphere");
}
